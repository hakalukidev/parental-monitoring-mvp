name: parent_app
description: Safetly - Parent application for parental monitoring MVP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  flutter_secure_storage: ^9.2.2
  socket_io_client: ^2.0.3+1
  flutter_webrtc: ^0.12.6
  provider: ^6.1.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
